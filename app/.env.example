# Web build (default): leave this EMPTY. The server serves the app + API on the same origin, so the
# app uses relative /api paths — works over http/https behind any hostname (Nginx Proxy Manager, etc.).
# Native build only: set this to the API base, e.g. http://your-server:3000 (never commit the real value).
EXPO_PUBLIC_API_URL=
