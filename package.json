{
  "name": "fitsheet",
  "version": "0.1.0",
  "private": true,
  "description": "Personal fitness companion — calories, weight, workouts, walks, recipes, notes, analytics.",
  "license": "AGPL-3.0-only",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix app install",
    "dev:server": "npm --prefix server run dev",
    "dev:app": "npm --prefix app start",
    "migrate": "npm --prefix server run migrate",
    "test": "npm --prefix server test",
    "typecheck": "npm --prefix server run typecheck && (cd app && node_modules/.bin/tsc --noEmit)"
  }
}
